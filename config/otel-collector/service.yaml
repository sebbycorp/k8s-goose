apiVersion: v1
kind: Service
metadata:
  name: otel-fanout-collector
  namespace: agentgateway-system
  labels:
    app: otel-fanout-collector
spec:
  selector:
    app: otel-fanout-collector
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: 4317
      protocol: TCP
