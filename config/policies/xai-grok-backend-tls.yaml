apiVersion: gateway.networking.k8s.io/v1alpha3
kind: BackendTLSPolicy
metadata:
  name: xai-grok-backend-tls
  namespace: agentgateway-system
spec:
  targetRefs:
    - group: agentgateway.dev
      kind: AgentgatewayBackend
      name: xai-grok
  validation:
    wellKnownCACertificates: System
    hostname: api.x.ai
